<Page
    x:Class="UniviewCodeBuddyProxy.Views.TokensPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:UniviewCodeBuddyProxy.Controls"
    mc:Ignorable="d">

    <Grid RowSpacing="16" Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header row -->
        <Grid Grid.Row="0" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="令牌"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                       VerticalAlignment="Center"/>

            <!-- Login with Browser button -->
            <controls:GlassButton Grid.Column="1"
                                  Text="浏览器登录"
                                  Icon=""
                                  ButtonStyle="Primary"
                                  Click="OnLoginWithBrowser"/>

            <!-- Add Token button -->
            <controls:GlassButton Grid.Column="2"
                                  Text="添加令牌"
                                  Icon=""
                                  ButtonStyle="Secondary"
                                  Click="OnAddToken"/>
        </Grid>

        <!-- Token list or empty state -->
        <Grid Grid.Row="1">
            <!-- Token list -->
            <ListView x:Name="TokenList"
                      ItemsSource="{x:Bind ViewModel.Tokens, Mode=OneWay}"
                      SelectionMode="None"
                      IsItemClickEnabled="False">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <controls:GlassCard CornerRadius="12">
                            <Grid ColumnSpacing="12" Padding="4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Status badge -->
                                <Border Grid.Column="0"
                                        CornerRadius="4"
                                        Padding="8,4"
                                        Background="{Binding StatusBadgeColor}">
                                    <TextBlock Text="{Binding StatusLabel}"
                                               FontSize="11"
                                               FontWeight="Medium"
                                               Foreground="White"/>
                                </Border>

                                <!-- Token info -->
                                <StackPanel Grid.Column="1" Spacing="2">
                                    <TextBlock Text="{Binding UserID}"
                                               FontSize="13"
                                               FontWeight="SemiBold"
                                               Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                                    <TextBlock FontSize="11"
                                               Foreground="{ThemeResource TextFillColorTertiaryBrush}">
                                        <Run Text="请求: "/>
                                        <Run Text="{Binding RequestCount}"/>
                                        <Run Text=" · 错误率: "/>
                                        <Run Text="{Binding ErrorRate}"/>
                                        <Run Text="%"/>
                                    </TextBlock>
                                </StackPanel>

                                <!-- Refresh button -->
                                <Button Grid.Column="2"
                                        Click="OnRefreshToken"
                                        Tag="{Binding Id}"
                                        ToolTipService.ToolTip="刷新"
                                        CornerRadius="6"
                                        Padding="8">
                                    <FontIcon Glyph="" FontSize="12"/>
                                </Button>

                                <!-- Delete button -->
                                <Button Grid.Column="3"
                                        Click="OnDeleteToken"
                                        Tag="{Binding Id}"
                                        ToolTipService.ToolTip="删除"
                                        CornerRadius="6"
                                        Padding="8">
                                    <FontIcon Glyph="" FontSize="12"/>
                                </Button>
                            </Grid>
                        </controls:GlassCard>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <!-- Empty state -->
            <StackPanel x:Name="EmptyState"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Spacing="12"
                        Visibility="Visible">
                <FontIcon Glyph="" FontSize="48"
                          Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                          HorizontalAlignment="Center"/>
                <TextBlock Text="尚未添加令牌"
                           FontSize="14"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="通过浏览器登录或手动添加令牌以开始使用。"
                           FontSize="12"
                           Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                           HorizontalAlignment="Center"
                           TextWrapping="Wrap"
                           MaxWidth="300"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
