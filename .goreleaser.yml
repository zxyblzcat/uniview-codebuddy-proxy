version: 2

builds:
  - id: codebuddy-proxy
    main: ./cmd/proxy
    env:
      - CGO_ENABLED=1
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X codebuddy-proxy/internal/version.Version={{.Version}} -X codebuddy-proxy/internal/version.Commit={{.Commit}} -X codebuddy-proxy/internal/version.Date={{.Date}}
    overrides:
      - goos: windows
        ldflags:
          - -s -w -H=windowsgui -X codebuddy-proxy/internal/version.Version={{.Version}} -X codebuddy-proxy/internal/version.Commit={{.Commit}} -X codebuddy-proxy/internal/version.Date={{.Date}}
    binary: codebuddy-proxy

archives:
  - id: default
    format: tar.gz
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}
    format_overrides:
      - goos: windows
        format: zip
    files:
      - README.md
      - LICENSE

checksum:
  name_template: "checksums.txt"

changelog:
  use: github-native

release:
  github:
    owner: zxyblzcat
    name: uniview-codebuddy-proxy
